import type { App } from 'vue'
import { RotateRightIcon as _RotateRightIcon } from './rotate-right'

const RotateRightIcon = {
  install: (app: App) => {
    app.component('RotateRightIcon', _RotateRightIcon)
  }
}

export default RotateRightIcon
