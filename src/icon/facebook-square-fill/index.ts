import type { App } from 'vue'
import { FacebookSquareFillIcon as _FacebookSquareFillIcon } from './facebook-square-fill'

const FacebookSquareFillIcon = {
  install: (app: App) => {
    app.component('FacebookSquareFillIcon', _FacebookSquareFillIcon)
  }
}

export default FacebookSquareFillIcon
